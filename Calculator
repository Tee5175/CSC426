# Display screen
entry Entry(root, width=20, font=("Arial", 24),
borderwidth=5, justify-RIGHT)
entry.grid(row=0, column=0, columnspan=4, padx=10, pady=10)
# Button click function
def click(value):
current = entry.get()
if value == "="
୦
try:
expression
current.replace(" "**"
"A" *").replace("\
\"
"//")
result = eval(expression)
entry.delete(0, END)
entry.insert(0, str(result))
except:
entry.delete(0, END)
entry.insert(0, "Error")
elif value == "C":
entry.delete(0, END)
else:
entry.insert(END, value)
# Calculator buttons
buttons = [
"7", "8", "9",
"/",
"4" "5" "6" "*"
"1","2" "3"
"0",
"%" "Λ"
"\\", "C", "="
"I
]
row = 1
col = 0
for button in buttons:
Button(
root,
text-button,
width=6,
height=2,
font ("Arial", 16),
command-lambda b-button: click(b)
).grid(row=row, column=col, padx=5, pady=5)
col += 1
if col > 3:
col = 0
row += 1
root.mainloop()
